// FullscreenGate - now just imports CSS for orientation lock
// No blocking, just CSS rotation
import './FullscreenGate.css';

const FullscreenGate = ({ children }) => {
  return children;
};

export default FullscreenGate;
